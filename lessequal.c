//Program to find if the values is lessthan or equal

#include <stdio.h>

int main() 
{
    int num1, num2;
    num1 = 10;
    num2 = 10;
    if (num1 <= num2) 
    {
        printf("%d is equal %d\n", num1, num2);
    } 
    else
    {
        printf("%d is less than %d\n", num1, num2);
    }

    return 0;
}